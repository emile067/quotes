import { MaxVotedDirective } from './max-voted.directive';

describe('MaxVotedDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxVotedDirective();
    expect(directive).toBeTruthy();
  });
});
